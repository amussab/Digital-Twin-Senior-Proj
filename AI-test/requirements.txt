# Install torch first, from the CPU index, or pip pulls a ~2.5 GB CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then:
#   pip install -r requirements.txt

pytorch-forecasting>=1.3
lightning>=2.2
pandas>=2.0
pyarrow>=14.0          # parquet
scikit-learn>=1.3      # classification metrics
scipy>=1.10            # dsp.py filters, CWRU .mat loading
matplotlib>=3.7

# ONNX export for the ASP.NET host. onnxruntime here is used only to verify
# the exported graph matches PyTorch; the deployed host uses the C# binding.
onnx>=1.15
onnxruntime>=1.17
